# /concepts

Reference HTML files from the design system. Source artifacts — not part of the build.
Use them as a visual + structural target when building React components in `/components`
and `/projects/<slug>`.

| File | What it covers | DESIGN-SYSTEM.md sections |
|---|---|---|
| `jr-mobile-home-concept-v3.html` | Full mobile home page composition: app head, hero, bento stat cards (J2), project list, quick links, floating bottom nav (I6), bento popover with 3x3 grid + scrollable lab list (5.3) | §3.6 stacking, §4.10–4.12, §5.2–5.4 |
| _missing_ `jr-round-1-v4.html` | Atomic UI: buttons (A), chips (B1), tags (B2), pills (C1), badges (C2), fields (D), selects (E), checks/radios (Fc), toggles (G), sliders (H) | §4.1–4.9 |
| _missing_ `jr-round-2-v2.html` | Surfaces + navigation: cards (J), bento (K), carousel (L), top/breadcrumb/tabs/sidebar nav (I1–I4), mobile top nav (I5) | §4.10–4.13, §5.1 |

Drop the missing files in this folder when ready and we'll port them too.
